# 需求理解文档

> **PRD来源**：{PRD文件路径}
> **分析日期**：{日期}
> **设计截图**：{截图列表，无则标注"无"}
> **模式**：{greenfield | brownfield | brownfield-pending}（greenfield = 全新项目；brownfield = 既有项目且 [CODE_BASELINE] 已生成；brownfield-pending = 既有项目但 [CODE_BASELINE] 还没产）

---

## 1. 需求简介

<!--
用一段话概括需求的核心目的和范围。应回答：
- 这个需求要解决什么问题？
- 目标用户是谁？
- 核心价值/目标是什么？
- 涉及哪些主要功能模块？
不超过200字。
-->

{一段话概括}

### 1.1 页面概况

<!--
描述本需求涉及的页面基本信息：
- 页面数量：几个页面
- 页面类型：H5 / 原生页面 / 小程序 / PC Web 等
- 涉及的端或角色：如用户端、主播端、商家端、管理后台等
- 多端差异：如果多端共用同一页面，明确各端的差异点（如某端隐藏某按钮、某端多一个入口等）
- 如果只有单端且无差异，简要说明即可
-->

- **页面数量**：
- **页面类型**：
- **涉及端/角色**：
- **多端差异**：

## 2. 核心逻辑

<!--
提取PRD中的核心业务逻辑和规则，包括：
- 关键业务流程（用编号步骤描述）
- 条件判断规则（if/then 形式）
- 约束条件和限制
- 优先级规则
用结构化方式呈现，避免大段文字。
-->

### 2.1 业务流程

1. ...
2. ...

### 2.2 业务规则

| 规则编号 | 条件 | 结果 | 备注 |
|---------|------|------|------|
| R-001 | ... | ... | ... |

### 2.3 约束条件

- ...

## 3. 实现范围

<!--
明确本次需求的边界，区分：
- 本次需要实现的（In Scope）
- 明确不在本次范围内的（Out of Scope）
- 依赖的前置条件或外部系统
-->

### 3.1 范围内 (In Scope)

- ...

### 3.2 范围外 (Out of Scope)

- ...

### 3.3 前置依赖

- ...

## 4. 页面布局

<!--
描述每个页面的布局结构，与设计截图对应。
- 如有截图，引用截图文件名（如：参见 home.png）
- 描述页面的主要区域划分
- 描述各区域的内容和层级关系
- 如有多个页面，分小节描述
- 如有同一页面的多状态截图，描述各状态下布局的变化
- 可使用 ASCII 布局图辅助说明
-->

### 4.1 {页面名称}

**对应截图**：{截图文件名}

**布局结构**：

```
┌──────────────┐
│  顶部区域    │
├──────────────┤
│              │
│  主内容区域  │
│              │
├──────────────┤
│  底部区域    │
└──────────────┘
```

**区域说明**：

| 区域 | 内容描述 | 备注 |
|------|---------|------|
| ... | ... | ... |

## 5. 交互需求列表

<!--
列出所有交互需求。每条交互需求包括：
- 编号：I-001 格式
- 交互元素：用户操作的UI元素
- 触发动作：用户执行的操作（点击、滑动、输入、长按等）
- 预期响应：系统的响应行为
- 来源：PRD中的具体出处，或从截图推断

注意：仅描述交互行为本身，不涉及如何实现。
-->

| 编号 | 交互元素 | 触发动作 | 预期响应 | 来源 |
|------|---------|---------|---------|------|
| I-001 | ... | ... | ... | PRD第X节 / 截图推断 |

### 5.1 关闭路径决策表（条件必填）

<!--
**触发条件**：当 §5 中任一 I-* 的"交互元素"是带「编辑沙盒 + 确认/取消」语义的容器（modal / drawer / popover / dialog / sheet / bottom-sheet / panel / wizard 等），必须为该容器穷举所有可能关闭路径，每条标注 commit / discard / no-op。

**不填的代价**：实现时遗漏关闭路径或语义反向（如把 × 当 save 实际应是 discard），导致用户操作不可恢复。属于 spec 漏洞，不是实现细节。

**穷举要求**：列出该容器在用户视角能触发关闭的所有动作 — 不限于以下集合，按容器特性补全：
- × / dismiss icon
- 蒙层（mask / backdrop）点击
- ESC 键
- Confirm / Save / OK / 主动作按钮
- Cancel / Discard / 副动作按钮
- 系统返回手势（移动端 swipe back / Android back button）
- 程序化关闭（点击外部 trigger 触发本容器关闭）

**每条路径必须显式标注 `commit` / `discard` / `no-op`**，不允许"未定义"或留空。如果 PRD 没明确说，必须在 §9 加 Q-XXX 待确认项问用户。
-->

| 关联 I-* | 容器类型 | 关闭路径 | 行为 | 来源 |
|---|---|---|---|---|
| I-XXX | drawer | × 按钮 | discard | PRD R-XXX / 待 PM |
| I-XXX | drawer | 蒙层点击 | discard | PRD R-XXX / 待 PM |
| I-XXX | drawer | Confirm 按钮 | commit | PRD R-XXX |
| I-XXX | drawer | ESC 键 | discard | 默认行为 |
| I-XXX | drawer | 系统返回手势 | discard | 平台默认 |

> 若本 PRD 不含编辑沙盒型容器，此节填 `N/A（本 PRD 无编辑沙盒型容器）`，不可省略此节。

## 6. 状态需求列表

<!--
列出所有状态需求。每条状态需求包括：
- 编号：S-001 格式
- 状态主体：拥有该状态的页面或组件
- 状态枚举：该主体的所有可能状态（用 / 分隔）
- **视觉变体集（state_variants）**：该状态主体作为原子 UI 元素的所有可能视觉变体（穷举要求 — 漏一种就漏一种功能）
- 各状态视觉表现：每种状态下 UI 的具体差异（哪些元素显示/隐藏、样式变化、文案变化等）
- 转换条件：状态之间的转换触发条件
- 默认状态：初始/默认状态
- 来源

包括但不限于：页面级状态（加载中/加载成功/加载失败/空数据）、组件级状态（选中/未选中/禁用）、业务状态等。

**视觉变体集穷举清单**（按需选取，不限于以下 — 业务态变体由 PRD 项目语境补充）：
- 通用：normal / hover / focus / pressed / disabled / readonly / selected / loading / error / empty / placeholder
- 业务态：项目相关的内容标记类（如 nsfw / verified / premium / locked / new-tag / muted 等）

**判定规则**：每个原子 UI 元素（chip / item / cell / list-row / button）必须列出**所有可能的视觉变体**。如果该元素没有变体（如纯文本），填 `only-normal`。漏列业务态变体（如 nsfw 标记）= spec 漏洞，不是实现细节。
-->

| 编号 | 状态主体 | 状态枚举 | 视觉变体集 | 各状态视觉表现 | 转换条件 | 默认状态 | 来源 |
|------|---------|---------|------------|---------------|---------|---------|------|
| S-001 | ... | 状态A / 状态B | normal / selected / disabled / nsfw | 状态A: xx显示, yy隐藏；状态B: xx隐藏, yy显示 | ... | 状态A | ... |

## 7. 数据需求列表

<!--
列出所有数据需求，按来源类型分类：

### 7.1 接口数据（API 返回的动态数据）
从服务端接口获取的数据，每条包括：
- 编号：D-001 格式
- 数据项：数据字段或数据集名称
- 数据类型：字符串、数字、列表、图片URL、日期等
- 展示位置：在哪个页面/区域中使用
- 备注：特殊格式要求、校验规则、默认值、是否可为空等

### 7.2 静态配置（链接、固定文案等）
页面中使用的静态配置数据，如跳转链接、规则页URL、固定文案等

### 7.3 客户端能力依赖（Bridge / JSBridge 调用）
页面需要调用的客户端原生能力，如拉起原生面板、调用相机、获取设备信息等。每条包括：
- 能力名称
- 调用场景：什么交互触发调用
- 传参：需要传递给客户端的参数
- 回调：客户端返回的结果（如有）
-->

### 7.1 接口数据

| 编号 | 数据项 | 数据类型 | 展示位置 | 备注 |
|------|-------|---------|---------|------|
| D-001 | ... | ... | ... | ... |

### 7.2 静态配置

| 编号 | 配置项 | 用途 | 备注 |
|------|-------|------|------|
| D-101 | ... | ... | ... |

### 7.3 客户端能力依赖

| 编号 | 能力名称 | 调用场景 | 传参 | 回调 |
|------|---------|---------|------|------|
| D-201 | ... | ... | ... | ... |

## 8. 动效需求列表

<!--
列出所有动画/动效需求。每条包括：
- 编号：A-001 格式
- 动效元素：涉及动效的UI元素
- 动效类型：过渡、入场、离场、循环、手势跟随、弹性等
- 触发条件：什么情况下触发该动效
- 动效描述：具体的动画效果描述（方向、时长、缓动等）
- 来源

如PRD中未提及任何动效需求，标注"本需求无动效需求"。
-->

| 编号 | 动效元素 | 动效类型 | 触发条件 | 动效描述 | 来源 |
|------|---------|---------|---------|---------|------|
| A-001 | ... | ... | ... | ... | ... |

## 9. 待确认项与疑问

<!--
列出分析过程中发现的、影响前端需求理解的待确认问题。
排除项：
- PRD与设计截图不一致的情况不属于待确认项（见§10）
- 服务端实现细节不属于前端待确认项。包括但不限于：数据分层策略、算法逻辑、后端配置规则、
数据条数/数量（由接口返回，前端按设计稿确定排布规则即可）等。前端仅关注接口返回的展示数据和设计稿确定的布局规则
此处仅收录：
- 编号：Q-001 格式
- 类别：
  - 歧义：PRD描述不清晰，存在多种理解方式
  - 遗漏：PRD或设计截图均未覆盖但应该覆盖的场景（如异常情况、边界条件）
  - PRD内部矛盾：PRD文本自身前后矛盾
  - 命名冲突：PRD 不同处对同一对象用了不同术语，或同一术语指代不同对象（如某状态在 A 处叫 "Draft"、B 处叫 "Unfinished"）—— 必须主动扫，不要等下游撞上
  - 端交互缺失：某端（PC / Mobile / 特定形态）的关键交互 PRD 未覆盖（如某编辑页在 PC 无独立返回入口、Mobile 手势 PRD 没写）—— 逐端核对页面级交互（返回 / 关闭 / 提交路径）是否每端都有定义
  - 建议：分析者基于经验提出的补充建议
- **主动扫描要求**：§9 不是"碰到才记"，生成 §9 前必须主动做两轮扫描 —— ① 术语一致性扫（同对象是否多名 / 同名是否多义）② 逐端交互完整性扫（每个页面的返回/关闭/提交在 PC 与 Mobile 是否都明确）。扫出的归入"命名冲突" / "端交互缺失"类。
- 问题描述：具体的问题内容
- **PRD 出处**（必填字段）：
  - 格式：`scoped:<起始行>-<结束行>`（仅行号 cursor，不含原文，便于 HTML 锚点跳转）
  - 无 PRD 依据的工程决策类 Q（项目侧约定 / 命名规范 / 灰度策略等）→ 必须显式标 `engineering-only`，禁止留空
  - 用途：主 Agent 收答后用本字段做"用户决策 vs PRD 原文"反向矛盾扫（见 `prd-clarifier.md` §主 Agent 接管模板 reconcile 强制扫）
- **PRD 原文片段**（必填字段 · 新增）：
  - 格式：30~80 字节 PRD 原话，用**单 backtick** inline code 包裹（如 `` `首页轮播图必须填，不能为空` ``）；要求**逐字摘抄不改写**，方便用户在 PRD 中 Cmd+F 命中
  - `engineering-only` 的 Q → 本列填 `—`（无原文可摘）
  - 用途：① `clarify-html-gen.mjs` 脚本渲染 HTML 时用 `<blockquote>` 展示这段原话给用户阅读；② 反向矛盾扫时用作语义对照锚点
- 建议：如有初步建议，可提供
-->

| 编号 | 类别 | 问题描述 | PRD 出处 | PRD 原文片段 | 建议 |
|------|------|---------|---------|------------|------|
| Q-001 | 歧义 | 主图能否为空？ | `scoped:210-214` | `首页轮播图必须填，不能为空` | 以 §3.2 为准，§7 接口字段改回必填 |
| Q-002 | 建议 | 灰度回滚阈值？ | `engineering-only` | `—` | 默认 50% 失败率自动回滚 |

## 10. 以设计稿为准的理解项

<!--
当PRD文本与设计截图存在不一致时，通常是因为设计稿产出过程中经过讨论对原始PRD做了调整，因此以设计稿为准。
这些差异不视为待确认项，而是在需求理解中直接采用设计稿的版本。此处汇总罗列所有以设计稿覆盖PRD的条目，供团队知悉。

每条包括：
- 编号：O-001 格式
- 差异点：简要描述不一致的内容
- **PRD 出处**（必填字段 · 新增）：格式 `scoped:<起始行>-<结束行>`，仅行号 cursor；用于 HTML 锚点跳转
- PRD描述：PRD中的原始表述（30~80 字节逐字摘抄，用**单 backtick** inline code 包裹，供 `clarify-html-gen.mjs` 渲染 HTML 时 `<blockquote>` 显示）
- 设计稿表现：设计截图中的实际表现（即本文档采用的版本）
- 涉及章节：该差异影响了本文档的哪些章节

如PRD与设计截图无不一致，标注"PRD与设计截图无不一致项"。
-->

| 编号 | 差异点 | PRD 出处 | PRD描述 | 设计稿表现（采用） | 涉及章节 |
|------|-------|---------|---------|-------------------|---------|
| O-001 | 按钮主色 | `scoped:118-120` | `按钮主色 #007AFF` | #5856D6（紫） | §4 |

---

## 11. 现有代码定位（brownfield）

<!--
本节仅 brownfield 模式填充（即 [CODE_BASELINE] 文档存在）。
greenfield / brownfield-pending 模式：写一行占位 `本节依赖 [CODE_BASELINE]，未提供 → 由未来 code-baseline 步骤补充（greenfield 项目可忽略）`，不主动 grep 仓库。

brownfield 模式填法：把 [CODE_BASELINE] 的代码盘点信息按本文档中的需求条目（R-XXX / I-XXX / S-XXX）重新索引，每条需求标注它对应的现有代码位置。
- 编号：C-001 格式
- 关联需求条目：本文档中对应的 R-XXX / I-XXX / S-XXX 编号
- 现有代码位置：文件路径（含必要时的行号）
- 当前实作状态（来自 [CODE_BASELINE]）：✅ 已实作 / ⚠️ 部分实作 / ❌ 未实作 / 📝 待验证
- 备注：如果是 ⚠️ / ❌，需说明缺失的部分
-->

| 编号 | 关联需求 | 现有代码位置 | 实作状态 | 备注 |
|------|---------|-------------|---------|------|
| C-001 | R-001 | src/components/X/index.tsx:NN | ⚠️ 部分实作 | ... |

## 12. 复用 vs 新建清单（brownfield）

<!--
本节仅 brownfield 模式填充。
greenfield / brownfield-pending 模式：写一行占位提示。

brownfield 模式填法：基于 §11 现有代码定位，把每条需求需要的代码动作拆成「复用」和「新建」两份清单。
- 复用清单（RU-XXX）：直接 import / 包装现有的组件 / atom / hook / util
- 新建清单（NW-XXX）：必须新写的文件（含粒度——是新文件、新函数还是新 props）

每条复用项必须给出：现有文件路径 + 复用方式（直接 import / 包装一层 / 仅传配置）
每条新建项必须给出：新文件预定路径 + 一句话职责
-->

### 12.1 复用清单

| 编号 | 关联需求 | 复用对象（现有文件） | 复用方式 | 备注 |
|------|---------|--------------------|---------|------|
| RU-001 | R-001 | src/components/CoUI/X.tsx | 直接 import | 已有 onceKey 模式可用 |

### 12.2 新建清单

| 编号 | 关联需求 | 新建文件 / 函数 | 一句话职责 | 备注 |
|------|---------|---------------|-----------|------|
| NW-001 | R-001 | src/.../FooBar.tsx | ... | 不能复用现有，原因：... |

## 13. 影响面评估（brownfield）

<!--
本节仅 brownfield 模式填充。
greenfield / brownfield-pending 模式：写一行占位提示。

brownfield 模式填法：列出会被改动的现有文件 / 组件 / 路由，以及对应的 e2e 回归路径。
- 编号：IM-001 格式
- 改动文件：现有文件路径
- 改动性质：新增逻辑 / 修改 props / 替换组件 / 删除等
- 关联需求：本文档中的 R-XXX / I-XXX / S-XXX
- 回归范围建议：哪些用户路径需要回归测试
-->

| 编号 | 改动文件 | 改动性质 | 关联需求 | 回归范围建议 |
|------|---------|---------|---------|-------------|
| IM-001 | src/.../X.tsx | 新增 props + 包裹 Tooltip | R-001 | 进入聊天页首屏 / 已访问过的回访 / 多账号切换 |

---

## 14. 项目级澄清默认值（缓存，T28）

<!--
本节用于把"在某个项目里跑 prd-clarifier 时大概率重复出现"的事实信息固化下来，下次主 Agent 重跑时自动采纳，避免每次都问用户同样的事情。

填写时机：
- prd-clarifier 收到用户答复后，识别其中与 PRD 无关、对项目通用的回答（项目级偏好 / 项目通用约定 / 项目技术栈选择），合并到本节
- 仅与本 PRD 某条编号绑定的答复 → 不写本节，仅写正文

填写格式（按需展开）：
- 14.1 项目级技术栈默认（框架 / 状态 / 样式 / 接口 / 平台等）
- 14.2 项目级复用基础（已存在的组件 / atom / 规则等，附路径）
- 14.3 项目级澄清问答默认（题 + 项目通用答；编号 C-D-001 起）
- 14.4 缓存维护说明

greenfield 项目 / 首次跑：本节可留空或仅写 14.1。
-->

### 14.1 项目级技术栈默认

| 主题 | 默认值 | 证据 / 引用 |
|------|--------|-----------|
| ... | ... | ... |

### 14.2 项目级复用基础

| 主题 | 现有资产 | 路径 | 用法摘要 |
|------|---------|------|---------|
| ... | ... | ... | ... |

### 14.3 项目级澄清问答默认

| 编号 | 题 | 默认答 |
|------|---|-------|
| C-D-001 | ... | ... |

### 14.4 缓存维护说明

主 Agent 在跑 prd-clarifier 时**先读本节**，命中默认的题自动 OK，没命中的才问用户。用户给的项目级答复要追加到本节。


